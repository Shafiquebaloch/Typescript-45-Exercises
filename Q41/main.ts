//Defining array
let magicansName=["Sahil", "sarfraz", "asad"];

//Making function to print Magicians Name
function showMagicians(megicians:string[]) {
   megicians.forEach(name=>console.log(name))
}

//Calling functuons
showMagicians(magicansName);
